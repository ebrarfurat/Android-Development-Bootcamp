<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Uygulama" parent="Theme.Material3.DayNight.NoActionBar">
        android:theme="@style/AppTheme"
        android:label="@string/app_name">
        <item name="android:statusBarColor">@color/yesil</item>
    </style>

    <style name="Theme.Uygulama" parent="Base.Theme.Uygulama" />
</resources>